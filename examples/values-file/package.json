{
  "name": "values-file",
  "version": "0.0.1",
  "description": "",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "generate": "rm -rf ./src/crds && cli generate-crd from-config-file ./crds-config.yaml --output ./src/crds",
    "start": "node dist/main.js",
    "generate-schema": "npx ts-json-schema-generator --path 'src/**/*.ts' --type 'Configuration' > schema.json"
  },
  "dependencies": {
    "@kubeframe/kubeframe-version": "file:../../core",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "class-validator-jsonschema": "^5.0.1",
    "commander": "^14.0.2",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@kubeframe/cli": "@kubeframe/cli:file:../../cli",
    "@types/node": "^22.13.5",
    "ts-json-schema-generator": "2.4.0",
    "typescript": "~5.9.3"
  },
  "keywords": []
}
